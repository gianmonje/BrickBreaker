public enum PaddleType {
    Wood
}

public enum BallType {
    Ball1
}

public enum LevelClearCondition {
    DestroyAllBricks,
    DestroyBricksOnList,
    DestroyCheckList
}

public enum GameResult {
    Win,
    Lose
}

public enum LockStatus {
    Unlocked,
    Locked
}

public enum CupCakeFlavor {
    Choco = 0,
    Mint = 1,
    Cheese = 2,
    Blue = 3,
    Strawberry = 4
}